@model SSDI_SPILELApplication.Models.BrowseStoryModel
@{
    ViewBag.Title = "ApproveContributedStories";
}
<h2>ApproveContributedStories</h2>
@{
    ViewBag.Title = "View All Stories";
    var index = 0;
}
<head>
    <script src="~/Scripts/angular.min.js"></script>
</head>
<body>
<div ng-app="BrowseStory">
    <form id="BrowseStories" name="BrowseStories" ng-controller="BrowseStoryController">

        <table class="table">

            <tr>
                <th style="text-align:center">Serial No.</th>
                <th style="text-align:center">Title</th>
                <th style="text-align:center">Content</th>
                <th style="text-align:center">Select Story</th>
            </tr>

            @for (int i = 0; i < Model.Stories.Count; i++)
            {
                <tr>
                    <td style="text-align:left">
                        <label>@(i + 1).</label>
                    </td>
                    <td style="text-align:left; margin:0px,0px,0px,0px">

                        <div style="text-align:center; overflow:auto; min-width:250px; max-height:50px;">
                            @Html.DisplayFor(modelItem => Model.Stories[i].Title)
                        </div>
                    </td>

                    <td style="text-align:center; overflow:auto">
                        <div style="text-align:center; overflow:auto; max-height:50px;">
                            @Html.DisplayFor(modelItem => Model.Stories[i].Content)
                        </div>
                    </td>
                    <td style="text-align:right">
                        <input type="button" value="Approve" onclick="window.location.href='@Url.Action("DeleteRestStories", "Home", new { storyID = Model.Stories[i].StoryID, ContributorID = Model.Stories[i].ContributorID})'" />
                    </td>


                </tr>
            }


        </table>

    </form>
</div>
</body>

